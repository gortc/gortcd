language: go

sudo: required

services:
  - docker

go:
  - 1.10.x
  - master

install:
  - make install

script:
  - make test
  - make lint
  - make test-e2e

after_success:
  - bash <(curl -s https://codecov.io/bash)

matrix:
  allow_failures:
    - go: master
  fast_finish: true
