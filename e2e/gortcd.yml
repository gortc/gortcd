---
version: "1.1"

server:
  workers: 2
  check_fingerprint: true
  listen:
    - 0.0.0.0:3478
  realm: realm

auth:
  public: false
  stun: false

  nonce:
    static: false
    timeout: 600s

  static:
    - username: username
      password: secret
